const { locations } = require('./src/app/trademark-by-city/locations.js');
// The file is locations.ts, so we can't easily require it directly in Node if it's TS.
// Let's just run a regex on it or compile it.
